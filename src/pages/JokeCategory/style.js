import { makeStyles } from '@material-ui/core';

export const style = makeStyles({

    container: {
        minHeight: '80vh',
    },

    searchBar: {
        padding: '1rem',
    },

    LoadingIcon: {
        marginLeft: '45%',
    },

});
