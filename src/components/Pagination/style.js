import { makeStyles } from '@material-ui/core';

export const style = makeStyles({
    container: {
        width: '100%',
        display: 'flex',
        justifyContent: 'space-between',
        margin: '0 auto',
    },

    button: {
        padding: '1rem',
    },
});
